<%= render 'shared/page_title', title: "Vytvorit novou ingredienci" %>

<%= render 'shared/errors', obj: @ingredient %>

<div class="row">
  <div class="well col-md-8 col-md-offset-2">
    <%= form_for @ingredient do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      
      <%= f.submit 'Zapiš ingredienci', class: "btn btn-success" %>
    <% end %>
  </div>
</div>